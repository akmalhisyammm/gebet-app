import React from 'react';
import Layout from 'components/layout';
import { TargetGebetanList } from 'components/targetSections';

const Target: React.FC = () => (
  <Layout title="Target Gebetan">
    <TargetGebetanList />
  </Layout>
);

export default Target;
