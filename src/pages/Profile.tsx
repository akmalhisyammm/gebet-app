import React from 'react';
import Layout from 'components/layout';
import { ProfileCard } from 'components/profileSections';

const Profile: React.FC = () => (
  <Layout title="Profil">
    <ProfileCard />
  </Layout>
);

export default Profile;
